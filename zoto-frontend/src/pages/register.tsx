import styles from '../styles/Register.module.css';

const Register = () => {
  return (
    <div className={styles.container}>
      <h1>顧客情報登録</h1>
      {/* 登録フォームの実装 */}
    </div>
  );
};

export default Register;
